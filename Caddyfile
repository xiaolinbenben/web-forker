wp.beisi.tech {
    encode gzip

    # block 无关 API 请求
    @badApi path /api/* /cart/* /collect /.well-known/*
    respond @badApi 204

    # WordPress rewrite
    @notFile {
        not file
    }
    rewrite @notFile /index.php

    # 代理到 Apache 80 端口
    reverse_proxy wordpress:80
}